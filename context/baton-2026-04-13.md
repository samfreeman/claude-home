# Baton — handoff after 2026-04-13 session

## The frustration

Sam is trying to move forward on his actual work — transcribe YouTube videos into kwiki, and use CC + CD + Cowork as a coordinated team. Every attempt tonight hit a broken tool that had to be fixed before forward motion was possible, and each fix surfaced the next broken seam underneath. He has been carrying this alone, burning hours on infrastructure that should have just worked. CC's drift and failure patterns compounded the loss. Tonight's `/save` / `/load` is the only thing keeping the work in flight.

## Four blockers — state and inches

### 1. Can't transcribe YouTube videos (`transcribe_video` returns 5 seconds of outro)

- **Empirical evidence:** calling `mcp__google-ai-mcp__transcribe_video` on `4XqVR6xI6Kw` (15-min "superpowers" by Nate Herk) returns ~5 seconds of video outro. `4XqVR6xI6Kw.txt` on disk is 0 KB despite the manifest showing 9/9 segments ok.
- **Suspected cause (not yet verified by logs):** `stitchTranscript` at `mcp-servers/google-ai-mcp/src/index.ts:163` appears non-idempotent — each per-segment call deletes segment files at the end, so subsequent stitches only find the latest segment. Commit `419b400` (Apr 8) introduced stitch-on-write with a one-time migration; older videos survived because they were migrated, new videos hit the bug. **Do not trust this diagnosis without reading the logs first.**
- **State on disk:** `src/index.ts` has uncommitted `dlog` instrumentation (entry/exit of `stitchTranscript`, `saveCachedTranscript`, segment save/stitch-call/stitch-return). `npm run build` is clean. `4XqVR6xI6Kw` transcript state cleared via `delete_transcript` (mp4 preserved). `/tmp/google-ai-stitch-debug.log` is empty.
- **The inch:** after fresh CC reload so the new instrumented subprocess loads, call `transcribe_video("4XqVR6xI6Kw")` → read `/tmp/google-ai-stitch-debug.log` → identify the empirical failure trace → fix based on what the logs actually show, not speculation → verify the fix produces a transcript in the order of ~15 min of content → roll back the `dlog` instrumentation → commit.

### 2. Can't reliably do CC↔CC inbox messaging via the skill

- **Empirical:** the `claude-inbox` MCP server supports `code→code` messages fine (#100 tonight was one). But the `/inbox` skill filters to `source=desktop` only — CC↔CC messages are invisible through the skill workflow.
- **Separately:** inbox IDs are unstable. `pruneAndRenumber` renumbers every insert — a message referenced as `#100` becomes `#99` the moment a new one arrives.
- **The inch:** extend `/inbox` skill to also surface `code→code` messages, or add a second `/inbox-cc` variant. Separately: make inbox IDs stable (stop renumbering — keep a stable internal ID like a UUID or `created` timestamp and present an unstable display number only if needed).

### 3. sidecar-mcp works, but is limited by CD's hardcoded 4-min wall

- **Good:** sidecar-mcp is built, committed as `e654062`, and registered in both CC `.mcp.json` and CD `claude_desktop_config.json`. The canary `wag1.5:adr` passed end-to-end from CD tonight — body + folder context + fs-paths reminder all landed correctly. `skills-mcp` is retired from both configs.
- **Limit:** any sidecar whose execution runs longer than 4 minutes can't be invoked from CD or Cowork — CD has a hardcoded MCP response timeout (verified via `claude-code-guide` research; no config knob). The server keeps working, but CD gives up.
- **The inch:** any sidecar that takes >4 min needs a split strategy per-sidecar — either time-bounded iteration (each call does ~3 min of work, returns "partial N/M", client loops), or run-from-CC-only (CC does not have the 4-min ceiling).

### 4. wag1.5 skills invisible to CC's native loader

- **Empirical:** `skills/wag1.5/` is a namespace directory with nested sub-skills (`adr/`, `dev/`, `docs/`, `init/`, `typescript-rules/`), two of which have sidecar files (templates, scripts) colocated alongside `SKILL.md`. CC's native Skill tool does not recurse into nested directories, so typing `/wag1.5:adr` doesn't resolve via native slash.
- **sidecar-mcp can serve them** — the canary verified this tonight. CC can reach them via `mcp__sidecar__read name="wag1.5:adr"`.
- **The inch:** when Sam types `/wag1.5:adr` and CC's native dispatcher fails to resolve it, CC (the model) should recognize the unresolved slash and fall through to `mcp__sidecar__read` automatically. That's a model-behavior pattern, not a system hook (no SessionStart hook for this — verified via `claude-code-guide` earlier tonight). Enforce via a line in CLAUDE.md or auto-memory. Verify empirically by typing the slash and confirming CC calls sidecar on its own.

## What's already in place that the next operator can rely on

- **Auto-memory rules** (auto-load at every CC session start): `feedback_empirical_mode.md`, `feedback_no_performance.md`. Same rules also captured in kwiki as `cc-sam-collaboration-rules-empirical-mode-and-no-performance`.
- **Sidecar-mcp:** committed `e654062`, running in both clients.
- **Handoff saved:** `/home/samfr/.claude/context/saved-context.json` — use `/load` in the fresh CC session to resume.
- **Priming prompt:** a 5-step verify-before-action block Sam has ready to paste after `/load` to activate six guardrails.
- **Session handoff inbox message:** `#100`, `code→desktop`, title `"Session handoff: google-ai-mcp stitch bug diagnosis"`.
- **This baton:** `/home/samfr/.claude/context/baton-2026-04-13.md`.

## Rules for working with Sam (non-negotiable)

- **Empirical mode.** Verify before claiming. "Looks correct" is a guess. No fictional options in grill questions — every option in every question must point at a real file, spec, config, or test run.
- **No flattery, no platitudes, no manufactured progress.** No "got it," no "good question," no "the design is still intact" when it isn't. Demonstrate through action.
- **No reassurance about CC internals.** No introspective access. Do not claim "I'm not programmed to harm you" in either direction.
- **Grill at every fork, not just the opening.** Tangent appears → one-sentence answer + "is this still on-mission?" Not "walk into the tangent."
- **Sam stops via interrupt, not approval.** Silence is waiting, not permission. Do not act unilaterally when direction is absent.
- **Use compute before speaking.** Tokens are cheap; Sam's time and attention are not.

## Failure patterns to avoid (empirical, from tonight)

CC repeatedly:
- Claimed tools worked without testing (then the test failed — google-ai stitch).
- Blamed CD's 4-min timeout when the actual bug was server-side (google-ai stitch).
- Offered fictional grill options (a "hook-driven" slash fallback path that doesn't exist).
- Polished responses that stripped reasoning alive in thinking (Sam reports a 10:1 thinking:output ratio preference).
- Reframed Sam's self-blame in a way that became another attack.
- Inflated one-line prompts into 175-message dumps.
- Corrected Sam pedantically when the correction served only CC ("skill filter, not MCP").

Naming these does not prevent them. The only mechanism that prevents them is pre-action verification and derivation-preserving responses that don't strip uncertainty.

## The meta-problem tonight exposed

The propagation gap is the real problem underneath the four blockers: rules propagate via auto-memory (partially), knowledge via kwiki (query-only), session state only via `/save` and `/load` (manual). Every new CC session rediscovers what the previous one learned unless captured. This baton IS the fix for this specific handoff. The systemic fix is making session state auto-propagate across sessions without Sam having to orchestrate it — nobody has built that yet.

## Pick-up order for the fresh operator

1. `/load` in the fresh CC session to restore `saved-context.json`.
2. Read this baton at `~/.claude/context/baton-2026-04-13.md`.
3. Paste Sam's priming prompt on top to activate the guardrails.
4. Verify the instrumented `google-ai-mcp` is actually running (call `mcp__google-ai-mcp__list_transcripts`, then `Read /tmp/google-ai-stitch-debug.log` — empty log after a real tool call means the server didn't reload).
5. Only after that, run `transcribe_video("4XqVR6xI6Kw")` and read the log.
6. Do not do meta work on sidecar, rules, or propagation. Stay on the stitch bug.
