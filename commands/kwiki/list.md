# kwiki list

List nodes and entries at a given path in the wiki.

## Arguments

$ARGUMENTS — optional path to list from (default: root)

## Instructions

1. Call `wiki_list` with the path from $ARGUMENTS (or root if none given).
2. Present the results clearly — child nodes and entries with their descriptions.
3. If a node has many entries, group or summarise them helpfully.
