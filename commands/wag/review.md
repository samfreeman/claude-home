---
description: Alias of /wag:rvw — senior architect review (RVW step of the cycle)
allowed-tools: Read, Write, Bash, Glob, Grep, Agent
---

This command is a synonym. Read `~/.claude/commands/wag/rvw.md` and follow it exactly as if the user had invoked `/wag:rvw`, passing through any arguments given here.