---
description: Alias of /wag:tri — the TRI (triage) phase
allowed-tools: Read, Write, Edit, Bash, Glob, Grep
---

This command is a synonym. Read `~/.claude/commands/wag/tri.md` and follow it exactly as if the user had invoked `/wag:tri`, passing through any arguments given here.